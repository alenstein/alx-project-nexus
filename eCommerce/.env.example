# SECURITY WARNING: keep the secret key used in production secret!
# You can generate a new secret key using:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your_secret_key_goes_here

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# A list of strings representing the host/domain names that this Django site can serve.
# Example for development: ALLOWED_HOSTS=localhost,127.0.0.1
# Example for production: ALLOWED_HOSTS=.yourdomain.com
ALLOWED_HOSTS=your,hosts,goes,here

# Database configuration
# Use this format: psql://db_user:db_password@db_host:db_port/db_name
DB_URL='db_url_goes_here'

# --- Email Configuration ---
# For production, use 'django.core.mail.backends.smtp.EmailBackend'
# For development, use 'django.core.mail.backends.console.EmailBackend'
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP settings for a real email service (e.g., SendGrid, Mailgun, AWS SES)
# These are ignored if EMAIL_BACKEND is set to console.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-smtp-username
EMAIL_HOST_PASSWORD=your-smtp-password
DEFAULT_FROM_EMAIL="Your Name <noreply@yourdomain.com>"
